FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /mock

COPY pyproject.toml README.md ./
COPY app ./app

RUN pip install --no-cache-dir .

EXPOSE 8001

CMD ["uvicorn", "app.mock_upstream:app", "--host", "0.0.0.0", "--port", "8001"]
